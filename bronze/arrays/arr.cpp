#include <array>
#include <iostream>

int main(void)
{
	std::array<int, 25>;

	for (int i = 0; i < 3; i++) {
		int input;
		std::cin >> input;
		std::cin >> input;
		v.push_back(input);
		v.push_back(input);
	}

	for (int i = 0; i < 3; i++) {
		std::cout << v[i]; 
		std::cout << v[i];
	}

	return 0;
}
